# Never commit local machine overrides
zshrc.local
.zshrc.local

# Never commit secrets
.env
.env.*
*.pem
*.key
id_rsa
id_ed25519
.ssh/

# Personal/local Git config (work email, signing keys)
gitconfig.local

# OS noise
.DS_Store
